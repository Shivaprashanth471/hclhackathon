apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-service
spec:
  selector:
    matchLabels:
      app: patient-service
  template:
    metadata:
      labels:
        app: patient-service
    spec:
      containers:
      - name: patient-service
        image: 
        envFrom:
        - configMapRef:
            name: orderservice-dev
        env:
          - name: TZ
            value: "Asia/Kolkata"        
          - name: ENV
            value: dev
          - name: PORT
            value: "80"
        ports:
          - containerPort: 80
        resources:
          limits:
            cpu: "300m"
            memory: "500Mi"
          requests:
            cpu: "200m"
            memory: "200Mi"
      imagePullSecrets:
      - name: regcreds            
---
apiVersion: v1
kind: Service
metadata:
  name: patient-service
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    app: patient-service    